<?xml version="1.0" encoding="UTF-8"?>
<configuration>
 
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>   
   
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>target/karate-core-test.log</file>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>    
   
    <logger name="io.karatelabs" level="INFO"/>
    <!-- change to DEBUG for troubleshooting tests if needed -->
    <logger name="karate.http" level="WARN"/>
    <logger name="karate.scenario" level="WARN"/>

    <!-- Suppress verbose Testcontainers stack traces when trying Docker connection strategies -->
    <logger name="org.testcontainers.dockerclient.DockerClientProviderStrategy" level="ERROR"/>

    <root level="warn">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="FILE" />
    </root>
  
</configuration>