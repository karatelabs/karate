language: java
dist: trusty
jdk:
  - oraclejdk9
sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer
install: true
script: mvn install -P pre-release -Dmaven.javadoc.skip=true -B -V

